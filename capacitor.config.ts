import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Mi_primer_proyecto',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
